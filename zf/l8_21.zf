# $Id$

include "tarski_def.zf".
include "tarski_prop_orthogonality.zf".
include "tarski_bet.zf".

#18_21_aux
assert forall (A B C : point). ~col A B C => (exists (P T : point). perp A B P A && col A B T && bet C T P).


goal forall (A B C : point). A!=B => (exists (P T : point). perp A B P A && col A B T && bet C T P).
